# Copy to .env.development / .env.production and adjust. Never commit secrets.
# Backend origin for REST calls (no trailing slash).
VITE_API_URL=http://localhost:8080
# "true" switches frontend services from localStorage demo data to the backend.
# Keep "false" until backend parity is proven for your flow.
VITE_USE_API=false
